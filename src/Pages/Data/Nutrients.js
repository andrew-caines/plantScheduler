
export default function Nutrients(props) { 
    return(<p>This will be where you add and remove Nutrients. They will have a Name, UPC code, and NPK values. When making an Event. You would pick the nurtient and the ml/l value.</p>)
}